import Month from "../month"

const juneContent = "in progress"

const images: any[] = []

const JunePage = () => {
	return <Month title="June" content={juneContent} images={images} />
}

export default JunePage
